name: Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build FCF
        run: |
           ${ANDROID_HOME}/cmdline-tools/latest/bin/sdkmanager --list
          
