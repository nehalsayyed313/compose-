name: Build

on: workflow_dispatch

jobs:
  running:
      runs-on: ubuntu-latest
      container: rocq/rocq-prover:latest
      steps:
        - name: building
          run: |
             git clone --depth 1 https://github.com/adampetcher/fcf.git
             cd fcf
             make -j 4
            
