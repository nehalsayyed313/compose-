name: build

on:
  workflow_dispatch: 

jobs:
  fedora-test:
    runs-on: windows-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: running 
        run: java --version 

      
          
